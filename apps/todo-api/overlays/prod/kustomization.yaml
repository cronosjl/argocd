apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
- ../../base

namespace: todo-api-prod

commonLabels:
  environment: prod
  tier: production

patches:
- path: patch-replicas.yaml
